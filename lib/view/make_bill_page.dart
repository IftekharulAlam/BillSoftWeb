import 'package:flutter/material.dart';

class MakeBillPage extends StatelessWidget {
  const MakeBillPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}